[project]
name = "crypto-vc-investment-database"
version = "1.0.0"
description = "Reproducible database of publicly known investments by twenty crypto venture capital funds"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31",
    "openpyxl>=3.1",
]

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
